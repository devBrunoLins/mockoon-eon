{
    "name": "api-mock",
    "private": true,
    "scripts": {
      "start": "mockoon-cli start --data ./mockoon.json --port $PORT --repair"
    },
    "dependencies": {
      "@mockoon/cli": "^8.0.0"
    }
  }
  
